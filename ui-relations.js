/* HİS Finans — Ana işlem / bağlı hareket UI V3
   Mevcut Supabase şemasını değiştirmez. Yeni tablo/kolon/RPC oluşturmaz. */
(function(){'use strict';
const escx=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
const norm=v=>String(v||'').toLocaleLowerCase('tr-TR').replace(/_/g,' ').trim();
const manualType=v=>{const k=norm(v);return !k||k==='manuel'||k==='manual'||k.includes('manuel banka')||k.includes('manuel kasa')||k.includes('manuel cari')||k==='cari hareket'||k==='kasa hareketi'||k==='banka hareketi';};
const isLinked=row=>!!row?.islem_id&&!manualType(row?.kaynak_turu);
const label=v=>{v=norm(v||'manuel');if(v.includes('transfer'))return ['↔','Para Transferi','transfer'];if(v.includes('tahsil'))return ['₺','Tahsilat','tahsilat'];if(v.includes('fatura'))return ['▧','Fatura','fatura'];if(v.includes('cek')||v.includes('senet'))return ['◈','Çek / Senet','cek'];return ['•','Manuel','manual'];};
const badge=v=>{const [i,t,c]=label(v);return `<span class="v4-source ${c}">${i} ${t}</span>`;};
const id8=id=>escx(String(id||'').slice(0,8));
function sourceBox(row){if(!isLinked(row))return `<div class="v4-relation"><span class="v4-source manual">• Manuel ana işlem</span>${row?.islem_id?`<span class="v4-sourceid">#${id8(row.islem_id)}</span>`:''}</div>`;const [,t]=label(row.kaynak_turu);return `<div class="v4-relation"><span>🔗</span>${badge(row.kaynak_turu)}<span class="v4-sourceid">#${id8(row.islem_id)}</span></div><button class="ghost v4-linkbtn" onclick='window.hisOpenSource(${JSON.stringify(row)})'>Ana işlemi gör →</button>`;}
function targetPage(row){const k=norm(row?.kaynak_turu);if(k.includes('transfer'))return 'transferler';if(k.includes('tahsil'))return 'tahsilatlar';if(k.includes('fatura'))return 'faturalar';if(k.includes('cek')||k.includes('senet'))return 'cekler';return null;}
function openSource(row){if(!isLinked(row)){toast('Bu hareket manuel ana işlemdir. Düzenleme ve silme bu hareket üzerinden yapılabilir.');return;}const p=targetPage(row);if(!p){toast('Ana işlem modülü bulunamadı.',true);return;}closeModal();S.page=p;location.hash=p;if(p==='transferler'&&window.transferPage)window.transferPage();else render();}
function prettyDbError(e,fallback){const m=String(e?.message||e||'');if(/foreign key|violates foreign key/i.test(m))return '🔒 Bu hesapta finans hareketleri bulunuyor. Önce hareketleri silin veya hesabı pasif hale getirin.';if(/permission|rls|row level/i.test(m))return 'Bu işlem için veritabanı yetkisi bulunmuyor.';return fallback;}
async function deleteManual(table,id){if(!confirm('Bu manuel finans hareketini silmek istiyor musun? Bakiye bu hareket kadar değişecektir.'))return;try{await api(`${table}?id=eq.${encodeURIComponent(id)}`,{method:'DELETE'});await load();toast('Manuel hareket silindi.');}catch(e){toast(prettyDbError(e,'Finans hareketi silinemedi.'),true);}}
async function deleteAccountSafe(table,id,movesTable,field,labelText){if(!confirm(`${labelText} hesabını silmek istiyor musun?`))return;try{const rows=await api(`${movesTable}?${field}=eq.${encodeURIComponent(id)}&select=id`);if(rows?.length){toast(`🔒 ${labelText} silinemedi. Bu hesaba ait ${rows.length} finans hareketi var. Önce hareketleri silin veya hesabı pasif hale getirin.`,true);return;}await api(`${table}?id=eq.${encodeURIComponent(id)}`,{method:'DELETE'});await load();toast(`${labelText} silindi.`);}catch(e){toast(prettyDbError(e,`${labelText} silinemedi.`),true);}}
function override(){
window.deleteBankMove=async function(id){const x=S.bankMoves.find(r=>String(r.id)===String(id));if(!x)return toast('Banka hareketi bulunamadı.',true);if(isLinked(x))return openSource(x);await deleteManual('banka_hareketleri',id);};
window.deleteCashMove=async function(id){const x=S.cashMoves.find(r=>String(r.id)===String(id));if(!x)return toast('Kasa hareketi bulunamadı.',true);if(isLinked(x))return openSource(x);await deleteManual('kasa_hareketleri',id);};
window.deleteCariMove=async function(id){const x=S.cariMoves.find(r=>String(r.id)===String(id));if(!x)return toast('Cari hareketi bulunamadı.',true);if(isLinked(x))return openSource(x);await deleteManual('cari_hareketleri',id);};
window.deleteCash=async id=>deleteAccountSafe('kasa_hesaplari',id,'kasa_hareketleri','kasa_id','Kasa');
window.deleteBank=async id=>deleteAccountSafe('banka_hesaplari',id,'banka_hareketleri','banka_hesap_id','Banka hesabı');
window.bankDetail=function(id){const b=S.banks.find(x=>x.id===id),rows=S.bankMoves.filter(x=>x.banka_hesap_id===id);modal('Banka Hareketleri',`<div class="detail"><div><small>Hesap</small><strong>${escx(b?.banka_adi||'')}</strong></div><div><small>Bakiye</small><strong>${money(bankBal(id))}</strong></div></div><div class="tableWrap"><table class="v4-table"><thead><tr><th>Tarih</th><th>Açıklama</th><th>Kaynak / Ana İşlem</th><th>Tip</th><th class="num">Tutar</th><th>İşlem</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${dt(x.tarih)}</td><td>${escx(x.aciklama||x.belge_no||'-')}</td><td>${sourceBox(x)}</td><td>${x.tip==='giris'?'<span class="goodPill">GİRİŞ</span>':'<span class="dangerPill">ÇIKIŞ</span>'}</td><td class="num">${money(x.tutar)}</td><td>${isLinked(x)?'<button class="v4-lock v4-lockbtn" onclick=\'window.hisOpenSource(${JSON.stringify(x)})\'>🔒 Bağlı →</button>':`<div class="v4-actions"><button class="ghost" onclick="editBankMove('${x.id}')">Düzenle</button><button class="dangerBtn" onclick="deleteBankMove('${x.id}')">Sil</button></div>`}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">Hareket yok.</td></tr>'}</tbody></table></div>`,closeModal,'Kapat');};
window.cashDetail=function(id){const b=S.cash.find(x=>x.id===id),rows=S.cashMoves.filter(x=>x.kasa_id===id);modal('Kasa Hareketleri',`<div class="detail"><div><small>Kasa</small><strong>${escx(b?.kasa_adi||'')}</strong></div><div><small>Bakiye</small><strong>${money(cashBal(id))}</strong></div></div><div class="tableWrap"><table class="v4-table"><thead><tr><th>Tarih</th><th>Açıklama</th><th>Kaynak / Ana İşlem</th><th>Tip</th><th class="num">Tutar</th><th>İşlem</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${dt(x.tarih)}</td><td>${escx(x.aciklama||x.belge_no||'-')}</td><td>${sourceBox(x)}</td><td>${x.tip==='giris'?'<span class="goodPill">GİRİŞ</span>':'<span class="dangerPill">ÇIKIŞ</span>'}</td><td class="num">${money(x.tutar)}</td><td>${isLinked(x)?'<button class="v4-lock v4-lockbtn" onclick=\'window.hisOpenSource(${JSON.stringify(x)})\'>🔒 Bağlı →</button>':`<div class="v4-actions"><button class="ghost" onclick="editCashMove('${x.id}')">Düzenle</button><button class="dangerBtn" onclick="deleteCashMove('${x.id}')">Sil</button></div>`}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">Hareket yok.</td></tr>'}</tbody></table></div>`,closeModal,'Kapat');};
window.cariDetail=function(id){const c=S.cariler.find(x=>x.id===id),rows=S.cariMoves.filter(x=>x.cari_id===id);modal('Cari Detay',`<div class="detail"><div><small>Cari</small><strong>${escx(c?.unvan||'')}</strong></div><div><small>Bakiye</small><strong>${money(cariBal(id))}</strong></div></div><div class="tableWrap"><table class="v4-table"><thead><tr><th>Tarih</th><th>Açıklama</th><th>Kaynak / Ana İşlem</th><th>Yön</th><th class="num">Tutar</th><th>İşlem</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${dt(x.tarih)}</td><td>${escx(x.aciklama||x.belge_no||'-')}</td><td>${sourceBox(x)}</td><td>${x.tip==='borc'?'Borç':'Alacak'}</td><td class="num">${money(x.tutar)}</td><td>${isLinked(x)?'<button class="v4-lock v4-lockbtn" onclick=\'window.hisOpenSource(${JSON.stringify(x)})\'>🔒 Bağlı →</button>':`<div class="v4-actions"><button class="ghost" onclick="editCariMove('${x.id}')">Düzenle</button><button class="dangerBtn" onclick="deleteCariMove('${x.id}')">Sil</button></div>`}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">Hareket yok.</td></tr>'}</tbody></table></div>`,closeModal,'Kapat');};
}
function install(){const css=document.createElement('style');css.textContent='.v4-sourcebox{padding:15px;border:1px solid #dbe5ef;border-radius:14px;background:#f8fbff;margin-bottom:12px}.v4-source-title{display:flex;align-items:center;gap:8px;flex-wrap:wrap;font-size:16px}.v4-source-help{color:#64748b;line-height:1.55;margin:12px 0}.v4-relation{display:flex;align-items:center;gap:6px;flex-wrap:wrap}.v4-sourceid{font:11px ui-monospace,SFMono-Regular,Menlo,monospace;color:#64748b}.v4-linkbtn{margin-top:6px;padding:6px 9px;font-size:11px}.v4-lockbtn{border:0;cursor:pointer}.v4-table thead th{position:sticky;top:0;background:#fff;z-index:1}.v4-table tbody tr:hover{background:#f5f8ff}.v4-actions{display:flex;gap:7px;flex-wrap:wrap}.v4-actions button{min-height:38px}';document.head.appendChild(css);window.hisOpenSource=openSource;override();
/* Kasa/Banka kartlarındaki eski inline Sil fonksiyonunu da güvenli katmana yönlendir. */
document.addEventListener('click',function(e){const btn=e.target.closest('button');if(!btn)return;const text=(btn.textContent||'').trim().toLocaleLowerCase('tr-TR');if(text!=='sil')return;const card=btn.closest('.account');if(!card)return;const raw=btn.getAttribute('onclick')||'';const m=raw.match(/['\"]([0-9a-fA-F-]{20,})['\"]/);if(!m)return;if(location.hash==='#kasa'||document.querySelector('h1')?.textContent==='Kasa') {e.preventDefault();e.stopImmediatePropagation();window.deleteCash(m[1]);}else if(location.hash==='#bankalar'||document.querySelector('h1')?.textContent==='Bankalar'){e.preventDefault();e.stopImmediatePropagation();window.deleteBank(m[1]);}},true);
new MutationObserver(()=>{if(!window.hisOpenSource)window.hisOpenSource=openSource;}).observe(document.body,{subtree:true,childList:true});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();